<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top shadow-sm px-4">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center" href="/listings">
      <i class="fa-regular fa-compass fs-4 me-2 text-danger"></i>
      <b>WanderLust</b>
    </a>

    <!-- Navbar toggler (for mobile) -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible content -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <!-- Search Form -->
      <form
        class="d-flex align-items-center me-3"
        role="search"
        action="/listings/search"
        method="get"
        style="max-width: 350px;"
      >
        <input
          class="form-control me-2"
          type="search"
          name="q"
          placeholder="Search Location | Title | Price"
          aria-label="Search"
          required
        />
        <button class="btn btn-outline-success" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>

      <!-- Right-side buttons -->
      <ul class="navbar-nav align-items-center">
        <% if (!currUser) { %>
          <!-- Not logged in -->
          <li class="nav-item me-2">
            <a class="btn btn-outline-dark px-3" href="/signup">
              <i class="fa-solid fa-user-plus"></i> Sign up
            </a>
          </li>
          <li class="nav-item me-2">
            <a class="btn btn-dark px-3" href="/login">
              <i class="fa-solid fa-right-to-bracket"></i> Log in
            </a>
          </li>
        <% } else { %>
          <!-- Logged in -->
          <li class="nav-item me-3">
            <span class="navbar-text">
              <i class="fa-solid fa-user"></i>
              <b><%= currUser.username %></b>
            </span>
          </li>
          <li class="nav-item me-2">
            <a class="btn btn-outline-danger px-3" href="/logout">
              <i class="fa-solid fa-arrow-right-from-bracket"></i> Log out
            </a>
          </li>
        <% } %>

        <!-- Airbnb your home (always visible) -->
        <li class="nav-item">
          <a class="btn btn-success px-3" href="/listings/new">
            <i class="fa-solid fa-circle-plus"></i> Airbnb your home
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>
